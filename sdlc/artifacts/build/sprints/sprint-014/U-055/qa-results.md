---
artifact_type: qa-results
story_id: "U-055"
verdict: "PASS"
agent: "QA Analyst"
timestamp: "2026-06-24"
ac_total: 5
ac_passed: 5
ac_failed: 0
tests_run: 690
tests_passed: 690
---

# QA Validation: U-055

## Verdict: PASS

Move androidx.security-crypto off alpha and add an encryption-scheme regression test.

Verified against actual files on the integrated tree. `security-crypto` is declared at `1.1.0`
stable (no alpha/beta qualifier). `verification-metadata.xml` pins the new artifact's SHA-256 with
explicit (non-wildcard) entries. The scheme-pinning regression test asserts AES-256-GCM/SIV at the
library-constant level and via a production-code capture path; production code at
`EncryptedPrefsSecretStore.java:110-121` directly uses AES256_GCM master key, AES256_SIV key
encryption, AES256_GCM value encryption with StrongBox best-effort retained. The credential
round-trip and degraded behavior remain intact.

## Acceptance Criteria Results

> Every PASS cites a `file:line`.

| AC | Status | Evidence (file:line references required) |
|----|--------|------------------------------------------|
| AC-1: version bumped to stable 1.x, no alpha/beta qualifier | PASS | `app/build.gradle:143` → `implementation 'androidx.security:security-crypto:1.1.0'`; no `alpha`/`beta`/`rc` token. ADR comment at `build.gradle:134-142`. |
| AC-2: verification-metadata updated with SHA-256 for new artifact + transitives; no wildcard | PASS | `gradle/verification-metadata.xml:686-693` — `security-crypto:1.1.0` AAR SHA-256 `2831ec1b...` (688) and module SHA-256 `2a04f7f9...` (691), explicit `origin="Generated by Gradle"`. No wildcard pattern covers these AAR/module artifacts (trusted-artifacts regex matches only `-javadoc`/`-sources` jars per U-056 review). Dep-verification PASS confirms checksums validate at build time. |
| AC-3: test pins MasterKey AES256_GCM + EncryptedSharedPreferences AES256_SIV/AES256_GCM | PASS | `EncryptedPrefsSchemeRegressionTest.java` — constant pins: `schemeRegression_prefKeyEncryptionScheme_mustBeAES256_SIV` (74-83), `..._prefValueEncryptionScheme_mustBeAES256_GCM` (91-100), `..._masterKeyScheme_mustBeAES256_GCM` (107-115); production-intent capture: `..._productionCode_usesAES256_SIV_keysAnd_AES256_GCM_values` (134-148). Production code verified at `EncryptedPrefsSecretStore.java:111` (MasterKey AES256_GCM), `:119` (AES256_SIV), `:120` (AES256_GCM). See Issues Found re: capture-path fidelity (non-blocking). |
| AC-4: existing EncryptedPrefsSecretStoreDegradedTest passes unmodified (API compat) | PASS | `EncryptedPrefsSecretStoreDegradedTest.java` present and unmodified; included in the 690-test green build. `EncryptedPrefsSecretStore.java` body unchanged (only Javadoc version note at `:33-39` updated). |
| AC-5: build green with new version + scheme test | PASS | Build report (execute-sprint context): `assembleDebug + testDebugUnitTest (690) + jacocoTestCoverageVerification + dep-verification` all PASS. Authoritative `@Test` count = 690 (`git grep @Test`). |

## Integration Path Verification

| Component | Entry Point | Call Path | Verified (yes/no) |
|-----------|-------------|-----------|--------------------|
| security-crypto:1.1.0 dependency | Gradle resolution | `build.gradle:143` declares; dep-verification validates SHA-256 from `verification-metadata.xml:686-693` | yes |
| EncryptedPrefsSecretStore (consumer of API) | `getEncrypted()` first use | `EncryptedPrefsSecretStore.java:110-121` builds `MasterKey.Builder` + `EncryptedSharedPreferences.create` — API surface unchanged across version bump (no code change) | yes |
| Scheme regression test | `:app:testDebugUnitTest` (Robolectric) | `EncryptedPrefsSchemeRegressionTest.java` auto-discovered in preferences test package; 5 `@Test` methods | yes |

## Behavioral Contract Verification

| Contract | Clause | Implementation (file:line) | Match (yes/no) |
|----------|--------|----------------------------|----------------|
| CNTR-MODERNIZATION-003 (SecretStore) | StrongBox best-effort MasterKey retained | `EncryptedPrefsSecretStore.java:112` `setRequestStrongBoxBacked(true)` unchanged | yes |
| CNTR-MODERNIZATION-003 | commit() durability for credential writes unchanged | `EncryptedPrefsSecretStore.java:163,173,189` still use `.commit()` | yes |
| Encryption scheme (AES-256-GCM/SIV) | scheme constants unchanged after bump | `EncryptedPrefsSecretStore.java:111,119,120` | yes |
| Data-at-rest compatibility | alpha06 ciphertext readable under 1.1.0 (same Tink AES-256-GCM/SIV) | No file rename, same `CREDENTIALS_FILE_NAME`/scheme; AC-4 degraded suite passes unmodified | yes |

## Requirement Scope Coverage

> Source: assessment:20260623-post-migration-assessment#SE-003 + BT-004 (merged). No formal REQ-*/DES-* in frontmatter.

| Requirement | Scope Item | Covered (yes/no) | Evidence (file:line) |
|-------------|------------|-------------------|----------------------|
| SE-003 / BT-004 | Move credential-encryption critical path off alpha artifact | yes | `build.gradle:143` `1.1.0` stable |
| SE-003 / BT-004 | Pin new artifact checksums (supply-chain integrity) | yes | `verification-metadata.xml:686-693` |
| SE-003 / BT-004 | CI guard against future scheme downgrade | yes | `EncryptedPrefsSchemeRegressionTest.java` (5 tests) |
| Preserve | StrongBox best-effort + commit() durability + migration path | yes | `EncryptedPrefsSecretStore.java:112,163,274-286` unchanged |

## Test Results

- Authoritative `@Test` count: **690**, all passing.
- `EncryptedPrefsSchemeRegressionTest.java`: 5 tests (3 constant-pins, 1 production-capture, 1 round-trip).
- `EncryptedPrefsSecretStoreDegradedTest.java`: present, unmodified, passing (API-compat regression guard).

## Regression Results

- `EncryptedPrefsSecretStore.java` production body unchanged (Javadoc-only edit) — credential round-trip and degraded behavior intact.
- Tink upgraded 1.7.x → 1.8.0 (same key-material format, backward-compatible at ciphertext layer per scheme constancy).
- alpha06 entries retained in `verification-metadata.xml:694-701` — harmless; Gradle does not auto-remove superseded versions.

## Issues Found

- **Test fidelity (Low, non-blocking, documented):** `SchemeCapturingStore.getEncrypted()` (`EncryptedPrefsSchemeRegressionTest.java:209-224`) re-declares the scheme constants locally rather than delegating to `super.getEncrypted()`, so the 2 builder-capture tests verify the test subclass's intent, not production delegation directly. Mitigated because (1) the 3 constant-pinning tests genuinely break if the library renames/weakens a constant, and (2) production code at `EncryptedPrefsSecretStore.java:111,119,120` is directly readable and unambiguously uses AES256_GCM/SIV/GCM — independently verified during this QA. AC-3 ("test asserts the scheme") is satisfied by the constant-pins + verified production code. Recorded in `U-055/review-security.md` Low finding with a refactor recommendation.
- **No on-device ACs** in this story.

## Phase Completion Report
---
story_id: "U-055"
phase: "validation"
verdict: "PASS"
artifact_path: "sdlc/artifacts/build/sprints/sprint-014/U-055/qa-results.md"
story_status: "validated"
current_build_phase: "validation"
ac_passed: 5
ac_total: 5
errors: []
---
